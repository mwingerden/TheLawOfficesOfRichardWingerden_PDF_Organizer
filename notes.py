# add a check for one Cert of Trust.

# add logic to learn if the files are a single or joint client.

# add a check for all expected files, throw error if program cannot find a file.
#   Ex: Cert of Trust is missing, throw error saying that Cert of Trust is missing.
